# Contest tracks

Drop hosted audio here (mp3, wav, or ogg), then register each file in `src/audio/tracks.ts`.

Example filenames: `house.mp3`, `deep.mp3`, `blend-a.mp3`, `blend-b.mp3`.

Without files on disk, catalog rows that declare a `synth` style still play via the procedural bed fallback.
